def sum(a,b)
{
  echo " The sum of the two no ${a} and ${b} is ${a+b} "
}
def mutiply(a,b)
{
  echo " The mutiply of the two no ${a} and ${b} is ${a*b} "
}
def subtract(a,b)
{
  echo " The subtraction of the two no ${a} and ${b} is ${a-b} "
}
